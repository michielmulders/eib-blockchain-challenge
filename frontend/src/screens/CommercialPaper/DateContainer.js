import React from 'react';

export default ({ title, value }) => (
    <div
        className="date--container-item"
    >
        <div className="date--container-title">{title}</div>
        <div className="date--container-value">{value}</div>
    </div>

);


